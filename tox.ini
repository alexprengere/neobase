[tox]
isolated_build = true
skip_missing_interpreters = true
envlist =
    py{39,310,311,312,313,314}
    pypy{39,310,311}

[testenv]
package = wheel
wheel_build_env = .pkg
dependency_groups =
    test
commands = py.test {posargs}

[testenv:coverage]
basepython = python3.14
commands = py.test --cov=neobase --cov=tests --cov-context=test --cov-report html --benchmark-skip {posargs}

[testenv:ruff]
basepython = python3.14
deps = ruff
commands = ruff check .

[testenv:ruff-format]
basepython = python3.14
deps = ruff
commands = ruff format . --line-length 88

[testenv:black]
basepython = python3.14
deps = black
commands = black .

[testenv:packages]
allowlist_externals =
    rm
basepython = python3.14
commands =
    rm -rf build src/*.egg-info
    uv build --sdist --wheel -o dist
